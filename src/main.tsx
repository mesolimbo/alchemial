import React from 'react'
import ReactDOM from 'react-dom/client'
import ElementalAlchemy from '../elemental-alchemy-game.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ElementalAlchemy />
  </React.StrictMode>,
)
